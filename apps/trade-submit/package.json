{
  "name": "@ussa/trade-submit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "USSA Lore Master Core — trade route submission (web form + bot-style command parser). Phase 3.",
  "scripts": {
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts",
    "demo": "tsx src/demo.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@fastify/static": "^7.0.4",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
